# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# "DS store" file types are Mac only and not necessary for Git file tracking.
# Run the following line on Mac computers that are apart of this repo if .DS_Store files pop back up
# find . -name .DS_Store -print0 | xargs -0 git rm --ignore-unmatch
*.DS_Store